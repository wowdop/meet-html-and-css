# Meet the Good to know tags: kbd, map, mark, math, meter, object, output,progress, q, ruby, samp, var & wbr

## Research

Good to know tags in html

[kbd tag html](https://www.google.com/search?q=kbd+tag+html)
and
[map tag html](https://www.google.com/search?q=map+tag+html)
and
[mark tag html](https://www.google.com/search?q=mark+tag+html)
and
[math tag html](https://www.google.com/search?q=math+tag+html)
and
[meter tag html](https://www.google.com/search?q=meter+tag+html)
and
[object tag html](https://www.google.com/search?q=object+tag+html)
and
[output tag html](https://www.google.com/search?q=output+tag+html)
and
[progress tag html](https://www.google.com/search?q=progress+tag+html)
and
[q tag html](https://www.google.com/search?q=q+tag+html)
and
[ruby tag html](https://www.google.com/search?q=ruby+tag+html)
and
[samp tag html](https://www.google.com/search?q=samp+tag+html)
and
[var tag html](https://www.google.com/search?q=var+tag+html)
and
[wbr tag html](https://www.google.com/search?q=wbr+tag+html)
and
[menu tag html](https://www.google.com/search?q=menu+tag+html)
and
[base tag html](https://www.google.com/search?q=base+tag+html)
and
[noscript tag html](https://www.google.com/search?q=menu+tag+html)
and
[data tag html](https://www.google.com/search?q=menu+tag+html)
and
[time tag html](https://www.google.com/search?q=time+tag+html)
and
[bdo tag html](https://www.google.com/search?q=bdo+tag+html)
and
[bdi tag html](https://www.google.com/search?q=menu+tag+html)
and
[abbr tag html](https://www.google.com/search?q=abbr+tag+html)
and
[address tag html](https://www.google.com/search?q=address+tag+html)
and
[cite tag html](https://www.google.com/search?q=cite+tag+html)
and
[details tag html](https://www.google.com/search?q=details+tag+html)
and
[dfn tag html](https://www.google.com/search?q=dfn+tag+html)
and
[dl tag html](https://www.google.com/search?q=dl+tag+html)
and
[ins tag html](https://www.google.com/search?q=ins+tag+html)
and

Also found

HTML Tags acording to "w3schools"

[html kbd tag w3](https://www.w3schools.com/tags/tag_kbd.asp)
and
[html map tag w3](https://www.w3schools.com/tags/tag_map.asp)
and
[html mark tag w3](https://www.w3schools.com/tags/tag_mark.asp)
and
[html meter tag w3](https://www.w3schools.com/tags/tag_meter.asp)
and
[html object tag w3](https://www.w3schools.com/tags/tag_object.asp)
and
[html output tag w3](https://www.w3schools.com/tags/tag_output.asp)
and
[html progress tag w3](https://www.w3schools.com/tags/tag_progress.asp)
and
[html q tag w3](https://www.w3schools.com/tags/tag_q.asp)
and
[html ruby tag w3](https://www.w3schools.com/tags/tag_ruby.asp)
and
[html samp tag w3](https://www.w3schools.com/tags/tag_samp.asp)
and
[html var tag w3](https://www.w3schools.com/tags/tag_var.asp)
and
[menu tag html](https://www.w3schools.com/tags/tag_menu.asp)
and
[base tag html](https://www.w3schools.com/tags/tag_base.asp)
and
[noscript tag html](https://www.w3schools.com/tags/noscript_kbd.asp)
and
[data tag html](https://www.w3schools.com/tags/tag_data.asp)
and
[time tag html](https://www.w3schools.com/tags/tag_time.asp)
and
[bdo tag html](https://www.w3schools.com/tags/tag_bdo.asp)
and
[bdi tag html](https://www.w3schools.com/tags/tag_bdi.asp)
and
[abbr tag html](https://www.w3schools.com/tags/tag_abbr.asp)
and
[address tag html](https://www.w3schools.com/tags/address_kbd.asp)
and
[cite tag html](https://www.w3schools.com/tags/tag_cite.asp)
and
[details tag html](https://www.w3schools.com/tags/tag_details.asp)
and
[dfn tag html](https://www.w3schools.com/tags/tag_dfn.asp)
and
[dl tag html](https://www.w3schools.com/tags/tag_dl.asp)
and
[ins tag html](https://www.w3schools.com/tags/tag_ins.asp)
and
[html wbr tag w3](https://www.w3schools.com/tags/tag_wbr.asp)

and Other sites:
[html math tag](https://iq.opengenus.org/using-mathml-in-html/)
[html output tag](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/output)

## Notes

More about the <ins>**`kbd`**</ins>, <ins>**`map`**</ins>, <ins>**`mark`**</ins>, <ins>**`math`**</ins>, <ins>**`meter`**</ins>, <ins>**`object`**</ins>, <ins>**`output`**</ins>, <ins>**`progress`**</ins>, <ins>**`q`**</ins>, <ins>**`ruby`**</ins>, <ins>**`samp`**</ins>, <ins>**`var`**</ins> & <ins>**`wbr`**</ins> tags in the examples underneath.

## Example list

### Example 1

```html:
...
 <p>
    Press <kbd>Command</kbd> + <kbd>V</kbd> to paste text from different
    sources.
  </p>
  <p>
    Code: Press &lt;kbd&gt;Command&lt;/kbd&gt; + &lt;kbd&gt;V&lt;/kbd&gt; to
    paste text from different sources.
  </p>
...
 <p>
    <label for="my-pie">Slices for Pie 1:</label>
    <meter id="my-pie" value="5" min="0" max="10">
      5 out of 10
    </meter>
  </p>
  ...
```

Visit the <ins>**`good-to-know/kbd-map-mark-math-meter.html`**</ins> file to find out how we made use of the <ins>**`kbd`**</ins>, <ins>**`map`**</ins>, <ins>**`mark`**</ins>, <ins>**`math`**</ins>, <ins>**`meter`**</ins> tags to tell the website how to display the code written inside the page.

### Example 2

```html:
...
 <object
    data="https://picsum.photos/500/300?random=1"
    width="500"
    height="300"
  ></object>
...
<form oninput="result.value=parseInt(a.value)+parseInt(b.value)">
      <input type="range" name="b" id="b" value="50" /> +
      <input type="range" name="a" id="a" value="10" /> =
      <output name="result" for="a b">60</output>
    </form>
...
<label for="steps-for-the-minimum-details">Personal Details:</label>
    <progress id="steps-for-the-minimum-details" value="70" max="100">
      70%
    </progress>
...
 <ruby>
      攻殻
      <rp>(</rp>
      <rt>こrうかく</rt>
      <rp>)</rp>
    </ruby>
...
```

Visit the <ins>**`good-to-know/object-output-progress-ruby.html`**</ins> file to find out how we made use of the <ins>**`object`**</ins>, <ins>**`output`**</ins>, <ins>**`progress`**</ins> & <ins>**`ruby`**</ins> tags to tell the website how to display the code written inside the page.

### Example 3

```html:
...
 <p>
    You can totally do this.
    <q>Click to find out more.</q>
    No pressure, no diamonds.
  </p>
...
 <p>When following the steps please make sure you don't get this error:</p>

    <p>
      <samp>
        Error "too much coding",
        <br />
        take a break you crazy geek.
      </samp>
    </p>
...
 <p>Another way of solving that is by using <var>x</var> * <var>y</var> = <var>z</var> syntax.</p>
...
<p>
      This one is how long it is: Fernstraßenbauprivatfinanzierungsgesetz
    </p>
    <p>
      This one is how you tell the browser to break it appart when the window is resizing:
      Fernstraßen<wbr>bau<wbr>privat<wbr>finanzierungs<wbr>gesetz
    </p>
...
```

Visit the <ins>**`good-to-know/q-samp-var-wbr.html`**</ins> file to find out how we made use of the <ins>**`q`**</ins>, <ins>**`samp`**</ins>, <ins>**`var`**</ins> & <ins>**`wbr`**</ins> tags to tell the website how to display the code written inside the page.

### Example 4

```html:
<p>
    When talking about <b><mark>Figma</mark></b
    >, here there are some examples of some short-cuts that you'll use:
  </p>

  <ul>
    <li>
      Placing a component inside a regular frame:
      <br />
      <kbd>command</kbd> + <kbd>option</kbd> + <kbd>G</kbd>
      <br />
      (<kbd>ctrl</kbd> + <kbd>alt</kbd> + <kbd>G</kbd>)
    </li>
    <li>
      Search in the Asset section:
      <br />
      <kbd>Option</kbd> + <kbd>2</kbd>
    </li>
    <li>
      Returns to the layers display:
      <br />
      <kbd>Option</kbd> + <kbd>1</kbd> = returns to the layers display.
    </li>
  </ul>

  <div>
    <label for="my-figma">Figma short-cuts examples read:</label>
    <meter id="my-figma" value="2" min="0" max="3">2 out of 3 read</meter>
  </div>

  <div>
    <label for="figma-short-cuts-steps">Personal Details:</label>
    <progress id="figma-short-cuts-steps" value="67" max="100">
      67%
    </progress>
  </div>
...
```

Visit the <ins>**`good-to-know/combo.html`**</ins> file to find out how we made use of the <ins>**`kbd`**</ins>, <ins>**`map`**</ins>, <ins>**`mark`**</ins>, <ins>**`math`**</ins>, <ins>**`meter`**</ins>, <ins>**`object`**</ins>, <ins>**`output`**</ins>, <ins>**`progress`**</ins>, <ins>**`q`**</ins>, <ins>**`ruby`**</ins>, <ins>**`samp`**</ins>, <ins>**`var`**</ins> & <ins>**`wbr`**</ins> tags to tell the website how to display the code written inside the page.

## Notes

### menu research

The `<menu>` tag defines a list/menu of commands.

The `<menu>` tag is used for context menus, toolbars and for listing form controls and commands.

The` <menu>` and `<ul>` elements both represent an unordered list of items. The key difference is that `<ul>` primarily contains items for display, while `<menu>` is intended for interactive items.

## Example list

### Example 1

```html:
<menu style="list-style: none">
  <li>
    <a href="#">Reload</a>
  </li>

  <li>
    <a href="https://mail.google.com/mail/u/0/#inbox">Email</a>
  </li>
</menu>
```

### Example 2

```html:
<menu style="list-style: none">
  <li>
    <a href="#">Profile</a>
  </li>

  <menu style="list-style: none">
    <li>
      <a href="#">Settings</a>
    </li>

    <li>
      <a href="#">Recover</a>
    </li>
  </menu>

  <li>
    <a href="#">Pages</a>
  </li>

  <menu style="list-style: none">
    <li>
      <a href="#">Home</a>
    </li>

    <li>
      <a href="#">News</a>
    </li>

    <li>
      <a href="#">Forum</a>
    </li>
  </menu>
</menu>
```

### base research

The `<base>` tag specifies the base URL and/or target for all relative URLs in a document.

The `<base>` tag must have either an href or a target attribute present, or both.

There can only be one single `<base>` element in a document, and it must be inside the `<head>` element.

| Attribute         | Value                            |
| ----------------- | -------------------------------- |
| <ins>href</ins>   | URL                              |
| <ins>target</ins> | \_blank, \_parent, \_self, \_top |

**href** specifies the base URL for all relative URLs in the page
**target** specifies the default target for all hyperlinks and forms in the page

#### target values explanations

- \_blank − the target URL will open in a new window.

- \_self − the target URL will open in the same frame as it was clicked.

- \_parent − the target URL will open in the parent frameset

- \_top − the target URL will open in the full body of the window

## Example list

### Example 1

```html:
<head>
  <base href="menu-base-experiment.html" target="_blank" />
</head>

<img src="../../../../public/image/html.jpeg" width="100" height="100" />

<p>
  <a href="menu-base-experiment.html">Click here</a> to load the same page, but
  in a different tab, even though this <b>a</b> tag has no
  <b>target</b> attribute.
</p>
```

### noscript research

The `<noscript>` tag defines an alternate content to be displayed to users that have disabled scripts in their browser or have a browser that doesn't support script. The `<noscript>` element can be used in both `<head>` and `<body>`.

## Example list

### Example 1

```html:
<head>
  <base href="menu-base-experiment.html" target="_blank" />
</head>

<img src="../../../../public/image/html.jpeg" width="100" height="100" />

<p>
  <a href="menu-base-experiment.html">Click here</a> to load the same page, but
  in a different tab, even though this <b>a</b> tag has no
  <b>target</b> attribute.
</p>
```

### data research

The `<data>` HTML element links a given piece of content with a machine-readable translation.

You can use it to display product names but also associate each name with a product number.

**value** is the attribute that it requires. The value of it should be the content for the machine that will read it.

## Example list

### Example 1

```html:
<p>New Products</p>
<ul>
 <li><data value="398">Mini Ketchup</data></li>
 <li><data value="399">Jumbo Ketchup</data></li>
 <li><data value="400">Mega Jumbo Ketchup</data></li>
</ul>
```

### time research

The `<time>` HTML element represents a specific period in time. It may include the datetime attribute to translate dates into machine-readable format, allowing for better search engine results or custom features such as reminders.

#### It may represent one of the following:

- a time on a 24-hour clock.
- a precise date in the Gregorian calendar (with optional time and timezone information).
- a valid time duration.

#### Attributes

<ins>datetime</ins> - his attribute indicates the time and/or date of the element and must be in one of the formats described below.

## Example list

### Example 1

```html:
<p>
  <i>The London Eye</i> will be reopened from
  <b>
    <time datetime="2018-07-07">July 7</time>
  </b>.
</p>

```

### Example 2

```html:
<p>
  Opening hours start at
  <b>
    <time datetime="09:00">09:00<time>
  </b> and you'll
  be able to enjoy the view for at least
 <b>
    <time datetime="PT2H30M">1h 30m</time>
  </b> per ticket bought.
</p
```

### bdo research

The `<bdo>` HTML element overrides the current directionality of text, so that the text within is rendered in a different direction.

#### Attributes

<ins>dir</ins> -The direction in which text should be rendered in this element's contents. Possible values are:

- <ins>ltr</ins>: Indicates that the text should go in a left-to-right direction.
- <ins>rtl</ins>: Indicates that the text should go in a right-to-left direction.

## Example list

### Example 1

```html:
<p>
  <bdo dir="rtl">This text will go right
to left.
  </bdo>
</p>

```

### Example 2

```html:
<p>
  <bdo dir="rtl">This text will go left to right. </bdo>
</p>
```

### bdi research

BDI stands for Bi-Directional Isolation.

The `<bdi>` tag isolates a part of text that might be formatted in a different direction from other text outside it.

The `<bdi>` element is used to wrap a span of text and instructs the bidirectional algorithm to treat this text in isolation from its surroundings. This works in two ways:

- the directionality of text embedded in `<bdi>` does not influence the directionality of the surrounding text.
- the directionality of text embedded in `<bdi>` is not influenced by the directionality of the surrounding text.

## Example list

### Example 1

```html:
<ul>
  <li>User <bdi>hrefs</bdi>: 13 projects</li>
  <li>User <bdi>jdoe</bdi>: 15 projects</li>
  <li>User <bdi>إيان</bdi>: 9 projects</li>
</ul>
```

### Example 2

```html:
<ul>
  <li><bdi>اَلأَعْشَى</bdi> - 1st place</li>
  <li><bdi>Jerry Cruncher</bdi> - 2nd place</li>
</ul>
```

### abbr research

The `<abbr>` tag defines an abbreviation or an acronym, like "HTML", "CSS", "Mr.", "Dr.", "ASAP", "ATM".

Tip: Use the global title attribute to show the description for the abbreviation/acronym when you mouse over the element.

#### Typical use cases

It's certainly not required that all abbreviations be marked up using `<abbr>`. There are, though, a few cases where it's helpful to do so:

- when an abbreviation is used and you want to provide an expansion or definition outside the flow of the document's content, use `<abbr>` with an appropriate title.
- to define an abbreviation which may be unfamiliar to the reader, present the term using `<abbr>` and either a title attribute or inline text providing the definition.
- when an abbreviation's presence in the text needs to be semantically noted, the `<abbr>` element is useful. This can be used, in turn, for styling or scripting purposes.

## Example list

### Example 1

```html:
<p>
  Ashok's joke made me
  <abbr title="Laugh Out Loud">LOL</abbr>
  big
time.
</p>
```

### Example 2

```html:
<p>
  <abbr title="HyperText Markup Language">HTML</abbr>
  is a markup language used to create the semantics and structure of a web page.
</p>
```

### address research

The `<address>` HTML element indicates that the enclosed HTML provides contact information for a person or people, or for an organization.

`<address>` can be used in a variety of contexts, such as providing a business's contact information in the page header, or indicating the author of an article by including an `<address>` element within the `<article>`.

#### Typical use cases

- the `<address>` element can only be used to represent the contact information for its nearest `<article>` or `<body>` element ancestor.
- this element should not contain more information than the contact information, like a publication date (which belongs in a `<time>` element).
- typically an `<address>` element can be placed inside the `<footer>` element of the current section, if any.

## Example list

### Example 1

```html:
<address>
	You can contact me for a website at
	<a href="http://www.somedomain.com/contact"> www.somedomain.com</a>.<br />
	If you see any bugs, please
	<a href="mailto:webmaster@somedomain.com"> contact webmaster</a>.<br />
	London<br />
	Canary Wharf, E13<br />
	UK
</address>
```

### Example 2

```html:
<footer>
  <address>
    &copy; Maria 2022
    <br />
    Send <a href="#"> an email</a>.<br />
    Phone number:
    <a href="tel:+447910185253">(791) 018 5253</a>
    <br />
    London<br />
    Canary Wharf, E13<br />
    UK
  </address>
</footer>
```

### cite research

The `<cite>` tag defines the title of a creative work (e.g. a book, a poem, a song, a movie, a painting, a sculpture, etc.).

Note: A person's name is not the title of a work.

The text in the `<cite>` element usually renders in italic.

## Example list

### Example 1

```html:
<p>
	<cite>Mona Lisa</cite>
	by Leonardo da Vinci. Painted in 1503.
</p>
```

### Example 2

```html:
<figure>
    <blockquote>
        <p>It was a bright cold day in April, and the clocks were striking thirteen.</p>
    </blockquote>
    <figcaption>First sentence in <cite><a href="http://www.george-orwell.org/1984/0.html">Nineteen Eighty-Four</a></cite> by George Orwell (Part 1, Chapter 1).</figcaption>
</figure>
```

### details research

The `<details>` tag specifies additional details that the user can open and close on demand.

The `<details>` tag is often used to create an interactive widget that the user can open and close. By default, the widget is closed. When open, it expands, and displays the content within.

Any sort of content can be put inside the `<details>` tag.

Tip: The `<summary>` tag is used in conjuction with `<details>` to specify a visible heading for the details.

#### Attributes

- open - this Boolean attribute indicates whether or not the details — that is, the contents of the `<details>` element — are currently visible. The details are shown when this attribute exists, or hidden when this attribute is absent. By default this attribute is absent which means the details are not visible.

## Example list

### Example 1

```html:
<details>
	<summary>WoW DoP</summary>

	<p>
		WoW DoP is the best place where you can start from scratch and learn the
		coding way to become a successfull web developer. Join the program and watch
		your life change in just a a matter of a few months
	</p>
</details>
```

### Example 2

```html:
<details open>
	<summary>Moving to London</summary>

	<p>
		John decided to change his life and move to London to follow his dream of
		becoming the most successfull web developer.
	</p>
</details>
```

### dfr research

#### Usage

- specifying the term being defined

- if the `<dfn>` element has a **title** attribute, the value of the title attribute is considered to be the term being defined. The element must still have text within it, but that text may be an abbreviation (perhaps using `<abbr>`) or another form of the term.

#### Attributes

- title - as explained above

## Example list

### Example 1

```html:
<p>
	The <strong>HTML Definition element</strong> (
	<strong> <dfn>&lt;dfn&gt;</dfn> </strong>) is used to indicate the term being
	defined within the context of a definition phrase or sentence.
</p>
```

### Example 2

```html:
<p>
	The <strong>HTML Definition element</strong> (
	<strong> <dfn>&lt;dfn&gt;</dfn> </strong>) is used to indicate the term being
	defined within the context of a definition phrase or sentence.
</p>

<p>
	Because of all of that, we decided to use the
	<code><a href="README.md">&lt;dfn&gt;</a></code> element for this project. if
	you click on the link, a page will be open with more details and explanations
</p>
```

### dl research

The `<dl>` tag defines a description list.

The `<dl>` tag is used in conjunction with `<dt>` (defines terms/names) and `<dd>` (describes each term/name).

## Example list

### Example 1

```html:
<dl>
	<dt>Firefox</dt>
	<dd>
		A free, open source, cross-platform, graphical web browser developed by the
		Mozilla Corporation and hundreds of volunteers.
	</dd>
</dl>
```

### Example 2

```html:
<dl>
	<dt>Firefox</dt>
	<dd>
		A free, open source, cross-platform, graphical web browser developed by the
		Mozilla Corporation and hundreds of volunteers.
	</dd>
</dl>
```

### Example 3

```html:
<dl>
	<dt>Firefox</dt>
	<dt>Mozilla Firefox</dt>
	<dt>Fx</dt>

	<dd>
		A free, open source, cross-platform, graphical web browser developed by the
		Mozilla Corporation and hundreds of volunteers.
	</dd>
</dl>
```

### Example 4

```html:
<dl>
	<dt>Firefox</dt>

	<dd>
		A free, open source, cross-platform, graphical web browser developed by the
		Mozilla Corporation and hundreds of volunteers.
	</dd>

	<dd>
		The Red Panda also known as the Lesser Panda, Wah, Bear Cat or Firefox, is a
		mostly herbivorous mammal, slightly larger than a domestic cat (60 cm long).
	</dd>
</dl>
```

### Example 5

```html:
<dl>
	<dt>Name</dt>
	<dd>Godzilla</dd>
	<dt>Born</dt>
	<dd>1952</dd>
	<dt>Birthplace</dt>
	<dd>Japan</dd>
	<dt>Color</dt>
	<dd>Green</dd>
</dl>
```

### Example 6

```html:
<dl>
	<div>
		<dt>Name</dt>
		<dd>Godzilla</dd>
	</div>

	<div>
		<dt>Born</dt>
		<dd>1952</dd>
	</div>

	<div>
		<dt>Birthplace</dt>
		<dd>Japan</dd>
	</div>

	<div>
		<dt>Color</dt>
		<dd>Green</dd>
	</div>
</dl>
```

### ins research

The `<ins>` tag defines a text that has been inserted into a document. Browsers will usually underline inserted text.

Tip: Also look at the `<del>` tag to markup deleted text.

#### Attributes

- **cite** - this attribute defines the URI of a resource that explains the change, such as a link to meeting minutes or a ticket in a troubleshooting system.

- **datetime** - this attribute indicates the time and date of the change and must be a valid date with an optional time string. If the value cannot be parsed as a date with an optional time string, the element does not have an associated time stamp. For the format of the string without a time, see Format of a valid date string. The format of the string if it includes both date and time is covered in Format of a valid local date and time string.

## Example list

### Example 1

```html:
<p>“You're late!”</p>
<del>
    <p>“I apologize for the delay.”</p>
</del>
<ins cite="../howtobeawizard.html" datetime="2022-07">
    <p>“A wizard is never late …”</p>
</ins>
```

@iulia-zburlea
@mariateo
